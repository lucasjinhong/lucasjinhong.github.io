source "https://rubygems.org"

# Specify Jekyll version (GitHub Pages often has a specific version, check their dependency versions page)
# gem "jekyll", "~> 3.9" # Example, check current GitHub Pages version if building for it specifically
# OR, use the github-pages gem which bundles compatible versions:
gem "github-pages", group: :jekyll_plugins # Recommended for GitHub Pages compatibility

# Specify the theme (if not handled by GitHub Pages settings directly)
gem "minima", "~> 2.5" # Check for the latest version of Minima

# Other plugins you might use (some are included in github-pages gem)
# gem "jekyll-feed"
# gem "jekyll-seo-tag"